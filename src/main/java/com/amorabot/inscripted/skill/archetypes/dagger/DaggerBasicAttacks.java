package com.amorabot.inscripted.skill.archetypes.dagger;

import com.amorabot.inscripted.skill.PlayerAbilities;
import com.amorabot.inscripted.skill.attackInstances.slash.Slash;
import com.amorabot.inscripted.skill.attackInstances.slash.SlashConfig;
import com.amorabot.inscripted.skill.attackInstances.slash.SlashPresets;
import com.amorabot.inscripted.skill.attackInstances.slash.SlashSegment;
import org.bukkit.entity.Player;
//import static com.amorabot.inscripted.skills.AbilityRoutines.newSwipeAttackBy;

public class DaggerBasicAttacks {

    public static void standardDaggerSlashBy(Player player, PlayerAbilities mappedAbility){
//        boolean isMirrored = Math.random() > 0.5;
//        boolean isInverted = Math.random() > 0.5;
//
//        Slash slash = new Slash(player, SlashPresets.STANDARD_DAGGER.getSlashConfigData(), false,
//                isMirrored,isInverted,false, 45);
//
//        slash.execute();
    }
}
